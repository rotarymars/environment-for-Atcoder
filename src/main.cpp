#include <iostream>
using namespace std;
#ifdef _DEBUG
#define DPln(x) cout<<#x<<" = "<<x<<endl
#else
#define DPln(x) ;
#endif
signed main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    
    return 0;
}
